apiVersion: v1
kind: Service
metadata:
  name: devsecops-project
  labels:
    app: devsecops-project
spec:
  type: NodePort
  ports:
  - port: 8080
    targetPort: 8080
    nodePort: 31000
  selector:
    app: devsecops-project